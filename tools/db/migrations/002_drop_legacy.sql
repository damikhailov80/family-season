-- Старая жизнь, где сезон хранился адресом.
--
-- К этому шагу её не читает уже никто: свои сезоны лежат в `user_seasons`,
-- выложенные — в `public_seasons`, а лайки, жалобы и избранное живут вокруг
-- второй таблицы. Всё, что было здесь, — адреса постеров, которые мы больше
-- не собираем: адрес теперь короткий и указывает на строку, а не возит бланк.
--
-- Отдельным шагом, а не вместе с заведением новых таблиц, намеренно: удалять
-- данные последним шагом безопаснее, чем первым. Между `001` и `002` умещался
-- весь переезд, и всё это время старое лежало нетронутым.

drop table if exists shared_reports;
drop table if exists shared_likes;
drop table if exists shared_seasons;
drop table if exists favorites;
drop table if exists seasons;
