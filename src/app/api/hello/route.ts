/**
 * Первый роут-хендлер: проверка, что серверная часть на Vercel действительно живёт.
 *
 * Лист приложения на бэкенд не опирается (он целиком в хэше ссылки), поэтому этот
 * маршрут ничего не знает о `Template` и намеренно ни на что в `src/model` не смотрит.
 */
export async function GET() {
  // GET-хендлеры в Next 15+ по умолчанию не кэшируются, так что время честно текущее.
  return Response.json({ ok: true, message: 'Привет из API', at: new Date().toISOString() })
}
