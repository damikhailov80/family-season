import type { ru } from './dict/ru'

/**
 * Форма словаря выводится из русского: он эталон. `as const` на нём намеренно
 * нет — он сузил бы значения до самих русских строк, и перевод перестал бы им
 * соответствовать.
 */
export type Dict = typeof ru
